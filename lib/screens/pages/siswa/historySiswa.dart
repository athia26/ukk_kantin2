import 'package:flutter/material.dart';

class Historysiswa extends StatelessWidget {
  const Historysiswa({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Ini history pemesanan"),
    );
  }
}